<UserControl x:Class="PasswordManager.View.DatabaseChallengeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding DatabaseChallenge, Source={StaticResource Locator}}">
    <Grid
        Background="White"
        Visibility="{Binding UserControlVisibility}">
        <StackPanel
                Orientation="Vertical">
            <TextBox
                Width="200"
                Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />
        
            <Button
                Width="100"
                Content="Open Database"
                Command="{Binding TryOpenDatabaseCommand}"
                IsEnabled="{Binding TryOpenDatabaseEnabled}"/>

            <TextBlock
                Text="{Binding Error}"
                Foreground="Red"/>
        </StackPanel>
    </Grid>
</UserControl>
